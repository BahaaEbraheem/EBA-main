@using OA.Domin.DSA
@using OA.Domin.Settings


<div class="form-group row">
    <div class="col-sm-6">
        <MInputText @bind-Value="NewCategory.Name" For="@(() => NewCategory.Name)" Id="Name" EntityType="typeof(NewCategory)" PropName="Name" Active="@Active" />
    </div>
    <div class="col-sm-6">
        <MInputNumber @bind-Value="NewCategory.Order" For="@(() => NewCategory.Order)" Id="Order" EntityType="typeof(NewCategory)" PropName="Order" />
    </div>
</div>

<div class="form-group row">
    <div class="col-sm-12">
        <MInputText @bind-Value="NewCategory.Descreption" For="@(() => NewCategory.Descreption)" Id="Descreption" EntityType="typeof(NewCategory)" PropName="Descreption" Active="@Active" />
    </div>
</div>

<div class="form-group row">
    <div class="col-sm-8">
        <UploadFile @ref="ImageUploadControl"
                    Label="Image"
                    Types='FileSettings.MimeTypes["Image"]'
                    ImagePreview="true"
                    ShowUploaded="false"
                    MaxSize="1024 * 1024 * 1"
                    OnValidUpload="@(() => NewCategory.Image = ImageUploadControl.Files[0] )"
                    OnStartPreUpload="@(() => UploadBusy = true)"
                    OnEndPreUpload="@(() => UploadBusy = false)" />
        @if (IsEdit)
        {
            <div>
                @NewCategory.ImagePath
            </div>
        }
    </div>
    <div class="col-sm-4 select-col">
        <CheckBoxBtn Checked="NewCategory.IsActive" Text="Active" OnChange="@((bool value) => NewCategory.IsActive = value)" />
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="M-center">
            @if (UploadBusy)
            {
                <MiniLoading />
            }
        </div>
    </div>
</div>

