@using OA.Domin.DSA.Indexes


<div class="form-group row">


    <div class="col-sm-6">
        <MInputText @bind-Value="ArrowType.Name_Ar" For="@(() => ArrowType.Name_Ar)" Id="Name_Ar" EntityType="typeof(ArrowType)" PropName="Name_Ar" Active="@Active" />
    </div>
    <div class="col-sm-6">
        <MInputText @bind-Value="ArrowType.Name_En" For="@(() => ArrowType.Name_En)" Id="Name_En" EntityType="typeof(ArrowType)" PropName="Name_En" Active="@Active" />
    </div>
</div>
    <div class="form-group row">
        <div class="col-sm-12">
            <MInputText @bind-Value="ArrowType.Description" For="@(() => ArrowType.Description)" Id="Description" EntityType="typeof(ArrowType)" PropName="Description" Active="@Active" />
        </div>
    </div>

